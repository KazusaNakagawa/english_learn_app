/// Application-level configuration constants.
///
/// Set voicevoxBaseURL and voicevoxApiKey from Config.xcconfig.
/// This file is listed in .gitignore and should not be committed with real values.
///
/// Setup:
///   1. Copy AppConfig.swift.example to AppConfig.swift (if not already done)
///   2. Deploy the CDK stack to get the API endpoint and key
///   3. Update Config.xcconfig with VOICEVOX_BASE_URL and VOICEVOX_API_KEY
enum AppConfig {
    /// Base URL for the VOICEVOX engine API (no trailing slash).
    /// Replace with your actual API Gateway endpoint after deploying the CDK stack.
    static let voicevoxBaseURL = "https://xxxxxxxxxx.execute-api.ap-northeast-1.amazonaws.com"

    /// API key for authenticating with the VOICEVOX backend.
    /// Get this from CDK deployment output (VoicevoxApiKey).
    static let voicevoxApiKey = "your-api-key-from-cdk-output-here"
}
